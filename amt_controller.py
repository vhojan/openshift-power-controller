def check_amt(ip):
    # Placeholder for AMT ping check
    return {"ip": ip, "reachable": True}

def power_control(ip, action):
    # Replace with actual AMT control code (e.g. using `pyamt`)
    return {"ip": ip, "action": action, "success": True}
